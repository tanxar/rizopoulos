# Environment Variables
# Copy this file to .env and fill in your values
# cp .env.example .env

# Server Port (Render uses PORT environment variable automatically)
PORT=3000

# Environment
NODE_ENV=development

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Session Secret (generate a random string, min 32 characters)
SESSION_SECRET=your-random-secret-key-change-this-in-production-min-32-chars

# Uploads Directory (for Render persistent disk - uncomment for Render)
# UPLOADS_DIR=/opt/render/project/src/uploads

# Database Path (for Render persistent disk - uncomment for Render)
# DATABASE_PATH=/opt/render/project/src/database.sqlite
